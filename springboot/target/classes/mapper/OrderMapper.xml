<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.suse.hwj.springboot.mapper.OrderMapper">
    <insert id="insertOrder" parameterType="com.suse.hwj.springboot.entity.Order" useGeneratedKeys="true" keyProperty="orderId">
        insert into `order` (order_no, user_id, course_id, coach_id, course_name, coach_name, price, order_time)
        values (#{orderNo}, #{userId}, #{courseId}, #{coachId}, #{courseName}, #{coachName}, #{price}, #{orderTime})
    </insert>
</mapper>